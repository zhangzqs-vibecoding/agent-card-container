# macOS M4 device evidence

Status: **NOT RUN**  
Commit: `<full commit SHA>`

Run `tooling/device-gates/macos-m4.sh <device-evidence.json>` on a supported macOS device. Evidence must prove embedded WKWebView loopback operation, per-instance origin/storage isolation, normal and detached windows, application signature and sandbox entitlements for the exact commit.

This template is not evidence of a pass.
